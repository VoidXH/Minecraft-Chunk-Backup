<Window x:Class="MinecraftChunkBackup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MinecraftChunkBackup"
        mc:Ignorable="d"
        Title="Minecraft Chunk Backup" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".3*"/>
            <ColumnDefinition Width=".7*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <GroupBox Header="Worlds" Margin="10,10,10,35"/>
        <Button Content="Add world" Margin="0,0,10,10" HorizontalAlignment="Right" Width="100" Height="20" VerticalAlignment="Bottom" Click="AddWorldButton"/>
        <GroupBox Grid.Column="1" Header="Regions" Margin="10,10,10,35">
            <DataGrid x:Name="regionList"/>
        </GroupBox>
        <Button Grid.Column="1" Content="Add regions" Margin="0,0,10,10" HorizontalAlignment="Right" Width="100" Height="20" VerticalAlignment="Bottom" Click="AddRegionsButton"/>
    </Grid>
</Window>